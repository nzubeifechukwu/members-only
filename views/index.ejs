<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Members Only</title>
  </head>
  <body>
    <nav><a href="/sign-up">Sign Up</a> <a href="/log-in">Log In</a></nav>
    <header><h1>Members Only</h1></header>
    <main>
      <section>
        <% if (messages.length) {%> <% messages.forEach((message) => { %>
        <article>
          <h2><%= message.title %></h2>
          <h3><%= message.first_name %></h3>
          <p><%= message.body %></p>
          <p><%= message.timestamp %></p>
        </article>
        <%}); %>
      </section>
      <% } else { %>
      <p class="">
        No messages found. <a href="/sign-up">Sign up</a> or
        <a href="/log-in">log in</a> to write a message.
      </p>
      <% } %>
    </main>
    <footer>
      <a href="" target="_blank"><small>&copy; Nzube Ifechukwu</small></a>
    </footer>
  </body>
</html>
